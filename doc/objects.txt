== room ==

from /src/room.h
depends on /src/room.cpp

external definitions:
    - "string"
    - uint (unsigned int)
    - seat_state(enum, values FREE, RESERVED, HANDED, LOCKED)
    - struct seat (seat_state, string)

public:
    - print_room()
    - lock()
    - unlock()
    - reserve()
    - release()
    - handout()
    - cancel()
private:
    - seat_input()
